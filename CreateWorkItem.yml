parameters:
  title: ''
  overview: ''
  cve: ''
  link: ''
  vulnerabilityTypes: []
steps:
- task: CreateWorkItem@1
  inputs:
    workItemType: 'Issue'
    title: 'Veracode SCA - ${{ parameters.title }}'
    fieldMappings: 'Overview: ${{ parameters.overview }} - CVE: ${{ parameters.cve }}VulnerabilityTypes: ${{ parameters.vulnerabilityTypes }} - Link: ${{ parameters.link }}'
    associate: true
    associationType: 'foundInBuild'
