parameters:
  sca_content: ''
  
steps:
- task: CmdLine@2
  name: SCAScan
  inputs:
    script: | 
      "{{ parameters.sca_content }}" | jq -c '.records[0].vulnerabilities[]' | while read vulnerability; do
          echo $vulnerability
      done
