parameters:
  sca_content: ''
  
steps:
- task: CmdLine@2
  name: CreateWorkItems
  inputs:
    script: | 
      "{{ parameters.sca_content }}" | jq -c '.records[0].vulnerabilities[]' | while read vulnerability; do
          echo $vulnerability
      done
